// ──────────────────────────────────────────────────────────────────────────
//  PAINEL DE CONTROLE — edite aqui sem mexer no resto do código.
// ──────────────────────────────────────────────────────────────────────────

// false = usa os clipes reais em public/footage/ (nomes em src/timeline.ts).
// true  = roda 100% com gradientes (sem nenhum vídeo). Deixe true até os
//         vídeos chegarem; vire para false quando colocar os .mp4 na pasta.
export const USE_PLACEHOLDER = true;

// true quando colocar a trilha em public/audio/track.mp3
export const HAS_AUDIO = false;

// Seus dados — aparecem nos cards de texto do trailer.
export const SPEAKER = '[SEU NOME]';
export const TAGLINE = 'Palestrante';
export const EVENT = '[NOME DO EVENTO]';
export const HANDLE = '@seu.instagram';

export const FPS = 30;
